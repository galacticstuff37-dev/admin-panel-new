export type BalanceCard = {
  label: string;
  value: string | number;
};

export type UserInfoRow = {
  label: string;
  value: string;
};

export type VerificationItem = {
  label: string;
  status: string;
  date?: string;
};
